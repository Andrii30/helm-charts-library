apiVersion: v1
kind: Service
metadata:
  name: {{ include "generic-webapp.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "generic-webapp.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
